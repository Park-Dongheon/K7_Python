#Chapter9 클래스

+ 객체 지향 프로그래밍(object-oriented programming, OOP)에서 실제 사물과 상황을 표현하는 클래스(class)를 만듦, 이 클래스를 기반으로 객체(object)를 만듦

+ 해당 객체 전체가 공유하는 일반적인 동작을 정의

+ 클래스에서 객체를 생성하면 각 객체는 자동으로 이 일반적인 동작을 상속, 각 객체가 가질 고유한 특징을 부여

+ 클래스에서 객체를 생성 => 인스턴스화(instantiation) 

